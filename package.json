{
  "name": "@sangotaro/monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "fmt": "prettier --ignore-path .gitignore -w .",
    "lint:fmt": "prettier --ignore-path .gitignore -c ."
  },
  "devDependencies": {
    "prettier": "2.4.1",
    "typescript": "4.4.3"
  },
  "engines": {
    "node": ">= 16"
  }
}
