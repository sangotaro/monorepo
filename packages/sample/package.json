{
  "name": "@sangotaro/sample",
  "private": true,
  "main": "dist/index",
  "types": "dist/index",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "npm run clean && npm run compile",
    "clean": "rm -rf ./dist && tsc -b tsconfig.build.json --clean",
    "compile": "tsc -b tsconfig.build.json"
  },
  "dependencies": {
    "@sangotaro/core": "*"
  }
}
